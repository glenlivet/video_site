
/**
 * The thrown exception when the external service provider changed the API
 *  and the change causes incompatible issue.
 */
export class ApiIncompatibleException extends Error {
  constructor() {
    super();
  }
}
